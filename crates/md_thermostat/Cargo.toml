[package]
name = "md_thermostat"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "Nose-Hoover NVT thermostat for MDDEM"
keywords = ["thermostat", "nose-hoover", "nvt", "molecular-dynamics"]
categories = ["simulation", "science"]

[dependencies]
mddem_core = { path = "../mddem_core", version = "0.1.0", default-features = false }
mddem_app = { path = "../mddem_app", version = "0.1.0" }
mddem_scheduler = { path = "../mddem_scheduler", version = "0.1.0" }
nalgebra = "0.33.2"
serde = { version = "1", features = ["derive"] }
